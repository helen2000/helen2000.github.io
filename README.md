# Museum Escape Room

# Install

- clone git repository
- run the following:

```
    sudo apt install nodejs
    npm install http-server
```

# Run

run is set up in package.json:

```
    npm start
```

or can use

```
    http-server .
```
